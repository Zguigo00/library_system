# DESIGN.md - 设计决策记录

## 2026-06-20 初始设计决策

### 项目定位
- **类型**：个人学习项目
- **目标**：掌握软件工程实践，提升代码健壮性
- **策略**：自底向上——先补细节（错误处理、业务规则），再逐步重构架构

### 开发方法
- **TDD（测试驱动开发）**：先写测试，再改代码
- **测试隔离**：内存数据库 `sqlite3.connect(":memory:")`
- **Database 类改造**：`__init__` 加 `db_path` 参数，默认 `"library.db"`
- **测试目录**：`tests/`，公共 fixture 放 `conftest.py`

### 业务规则决策

#### 借书
- 副本状态必须是"在馆"才能借出（当前缺失，需修复）
- 读者有逾期或欠费：不禁止，仅记录/显示（学习项目不需要太严格）
- 同一读者可借同名书的不同副本

#### 还书
- 还书前验证借阅记录属于当前读者（当前缺失，需修复）
- 已遗失的书允许归还，罚款不变（100元），副本状态改回"在馆"
- 超期罚款自动计算，但在 UI 显示金额

#### 删除约束
- 删除读者：有未还的书 → 禁止；无未还的书 → 允许，保留历史借阅记录
- 删除图书：有副本（在馆或已借出）→ 禁止；无副本 → 允许
- 删除副本：已借出 → 禁止；在馆 → 允许
- 历史借阅记录永久保留

### 实施顺序
1. 借书检查副本状态（`borrow_book`）
2. 还书验证归属（`return_book`）
3. 允许归还已遗失的书（`return_book`）
4. 删除读者前检查未还书籍（`delete_reader`）
5. 删除图书前检查副本（`delete_book`）
6. 删除副本前检查借出状态（`delete_copy`）

### 后续方向（暂定）
- 架构重构：MVC 分层，引入 Service 层
- 补充功能：输入验证、错误处理、日志
- 测试覆盖：逐步扩展到所有模块
